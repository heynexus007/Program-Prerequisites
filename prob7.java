import java.util.*;
//Perimeter of a Rectangle
public class prob4 {
    public static void main(String[] args) {
        Scanner ip=new Scanner(System.in);
        double length=ip.nextDouble();
        double width=ip.nextDouble();
		
		double Perimeter= (P*R*T)/100;
 
        System.out.println(Perimeter);
    }
}